#!/usr/bin/env ruby

$LOAD_PATH.unshift(File.expand_path('../lib', __dir__))

require 'github_score'

events = Github::Client::Activity::Events.new.public('Draiken').to_a
score = GithubScore::EventScorer.score(events)
puts("Total score for #{events.size} events\n#{score}")
